import client from '../api/client';

// TODO(sprint): implementar llamadas reales a /notifications (ver CLAUDE.md 5.5).
// Se deja el cliente ya importado para que el siguiente sprint solo agregue
// las funciones (getNotifications, create..., etc.) sin tocar api/client.js.

export default client;
